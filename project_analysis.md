# تحليل المشروع وهيكلة التحويل إلى برمجة خاصة (Custom Development Analysis)
**اسم المشروع:** موقع مجموعة 3M (3M Group)
**الهدف من التحويل:** الانتقال من نظام ووردبريس (WordPress/WooCommerce) الحالي إلى **موقع برمجة خاصة (Custom Application/CMS)** بتصميم فريد وعصري، حيث تُعرض **المنتجات في صفحات مخصصة ومستقلة** (Dedicated Product Pages) وليست كمنشورات/مدونات.

---

## 1. نظرة عامة على المشروع الحالي (Current System Audit)
* **النظام الحالي:** موقع تم بناؤه باستخدام WordPress، وتحديداً قالب Neve، مع استخدام أداة بناء الصفحات Elementor، وملحقات مثل WPForms و WooCommerce.
* **طبيعة النشاط:** شركة تجارية/صناعية (مجموعة 3M) متخصصة في المعدات الصناعية (مثل قطاعات الليزر المعدنية، اللوجستيات، المعدات التكنولوجية، والحلول الهندسية).
* **المحتوى الحالي:** 
  * صفحات تعريفية (من نحن، خدماتنا، اتصل بنا).
  * معرض صور وملفات وسائط (صور معدات، لوجوهات، شهادات).
  * منتجات ومواصفات تقنية.
* **أسباب الانتقال لبرمجة خاصة:**
  * التخلص من بطء ووردبريس والمشاكل الأمنية وتحديثات الإضافات المستمرة.
  * الحصول على **تصميم فريد كلياً (Custom UI/UX)** لا يتقيد بقوالب جاهزة.
  * هيكلة واضحة واحترافية للمنتجات في **صفحات مستقلة** لكل منتج، مع تسهيل تصفحها وطلبها.

---

## 2. متطلبات الفكرة الأساسية: "المنتجات في صفحات مخصصة"
بدلاً من عرض المنتجات بطريقة المنشورات أو المقالات، سيعتمد الموقع الجديد على هيكلة احترافية للمنتجات:
1. **صفحة المتجر / الكتالوج الرئيسي (Catalog/Products Index):**
   * عرض المنتجات في شبكة (Grid) أو قائمة منظمة مع صور واضحة وأسماء وتصنيفات فرعية.
   * شريط تصفية جانبي أو علوي (Filter) حسب الفئات، المواصفات، أو التطبيقات الصناعية.
2. **صفحة تفاصيل المنتج المستقلة (Dedicated Product Page):**
   * كل منتج يمتلك **صفحة خاصة به** (رابط مستقل مثل `domain.com/products/laser-cutter-3015`).
   * معرض صور احترافي للمنتج (صور متعددة مع إمكانية التكبير).
   * جدول أو قسم مخصص للمواصفات الفنية (Technical Specifications).
   * وصف تفصيلي للمنتج ومميزاته.
   * أزرار تفاعلية واضحة (مثل: "اطلب عرض سعر"، "استفسر عن المنتج عبر واتساب"، أو إضافة للسلة إن وجد بيع مباشر).
3. **قسم المدونة / المنشورات (قسماً منفصلاً تماماً):**
   * مخصص للأخبار، المقالات التقنية، ونصائح الشحن أو الصناعة، بعيداً عن المنتجات.

---

## 3. التقنيات المقترحة (Tech Stack)
لضمان الأداء العالي، الأمان، وسهولة الصيانة، يُقترح استخدام إحدى الكتل التقنية التالية:
* **الخيار الأول (Full-Stack PHP - Laravel):**
  * **Backend:** Laravel (PHP) - قوي، آمن، ممتاز لإدارة المحتوى والمنتجات.
  * **Frontend:** Blade + Tailwind CSS + Alpine.js (أو Vue.js) لتصميم سلس وسريع.
  * **Database:** MySQL / PostgreSQL.
* **الخيار الثاني (JavaScript Stack - MERN / Next.js):**
  * **Frontend:** Next.js (React) - ممتاز للـ SEO (محركات البحث) والسرعة الفائقة لصفحات المنتجات.
  * **Backend API:** Node.js (Express) أو NestJS.
  * **Database:** MongoDB أو PostgreSQL.

---

## 4. هيكل قاعدة البيانات المقترح (Database Schema)

### أ. جدول المستخدمين (Users / Admins)
* `id`: معرف فريد
* `name`: الاسم
* `email`: البريد الإلكتروني
* `password`: كلمة المرور المشفرة
* `role`: الصلاحية (مدير، محرر)
* `timestamps`

### ب. جدول تصنيفات المنتجات (Product Categories)
* `id`: معرف التصنيف
* `name`: اسم التصنيف (عربي/إنجليزي)
* `slug`: الرابط المختصر
* `image`: صورة التصنيف
* `description`: الوصف

### ج. جدول المنتجات (Products)
* `id`: معرف المنتج
* `category_id`: التصنيف الرئيسي
* `title`: اسم المنتج
* `slug`: الرابط الفريد (للصفحة المستقلة)
* `short_description`: نبذة مختصرة
* `description`: الوصف التفصيلي (HTML/Rich Text)
* `featured_image`: الصورة الرئيسية
* `gallery`: صور إضافية للمعرض (JSON array)
* `specifications`: المواصفات الفنية (JSON key-value مثل القوة، الأبعاد، الوزن)
* `is_featured`: منتج مميز (لعرضه في الصفحة الرئيسية)
* `status`: الحالة (منشور، مسودة)
* `views_count`: عدد المشاهدات
* `timestamps`

### د. جدول المقالات/المدونة (Posts - منفصل عن المنتجات)
* `id`: معرف المقال
* `title`: عنوان المقال
* `slug`: الرابط الفريد
* `content`: محتوى المقال
* `image`: صورة المقال
* `timestamps`

### هـ. جدول الاستفسارات وطلبات عروض الأسعار (Inquiries / Leads)
* `id`: معرف الطلب
* `product_id`: معرف المنتج المستهدف (اختياري)
* `name`: اسم العميل
* `phone`: رقم الهاتف
* `email`: البريد الإلكتروني
* `message`: نص الرسالة / الاستفسار
* `status`: حالة الطلب (جديد، تم الرد، قيد المعالجة)
* `timestamps`

---

## 5. ميزات لوحة التحكم المخصصة (Custom Admin Dashboard)
1. **إدارة المنتجات (Products Manager):**
   * إضافة منتج جديد مع تحديد التصنيف، الصور المتعددة، وإدخال المواصفات الفنية بحقول منظمة.
   * تعديل وحذف وإدارة حالة المنتجات بسهولة.
2. **إدارة التصنيفات والمدونة:** تنظيم فئات المنتجات وإدارة مقالات المدونة بشكل منفصل.
3. **إدارة الاستفسارات وطلبات عروض الأسعار (CRM):**
   * استقبال ومتابعة استفسارات العملاء القادمة من صفحات المنتجات، مع سهولة الرد أو تحديث الحالة.
4. **الإعدادات العامة للسيادة الرقمية (SEO & Settings):**
   * تعديل عناوين الأقسام، الكلمات المفتاحية لكل صفحة منتج، وروابط التواصل الاجتماعي.

---

## 6. خطة العمل ومراحل التنفيذ (Implementation Roadmap)

* **المرحلة الأولى: التخطيط وتصميم واجهات المستخدم (UI/UX Design)**
  * رسم مخططات الواجهات (Wireframes) لتصميم صفحات المنتجات المستقلة، الصفحة الرئيسية، وصفحة تفاصيل المنتج.
  * تصميم تجربة مستخدم (User Journey) تتيح تصفحاً سلساً للمعدات والمنتجات الصناعية.

* **المرحلة الثانية: تطوير الخلفية وقاعدة البيانات (Backend Development)**
  * بناء هيكل قاعدة البيانات الجديد الخاص بالمنتجات والصفحات المستقلة.
  * برمجة لوحة التحكم لإدارة المنتجات، المواصفات، والاستفسارات.

* **المرحلة الثالثة: تطوير الواجهات الأمامية (Frontend Development)**
  * تحويل التصاميم إلى أكواد HTML/CSS/JS متجاوبة وعالية الأداء.
  * ربط صفحات المنتجات وقوائم التصفية (Filtering) بـ Backend.

* **المرحلة الرابعة: ترحيل البيانات (Data Migration)**
  * استخراج بيانات المنتجات والصور والمواصفات من قاعدة بيانات ووردبريس القديمة ونقلها إلى الهيكل الجديد المنظم في صفحات مستقلة.

## 6. خطة العمل ومراحل التنفيذ (Implementation Roadmap)

* **المرحلة الأولى - الرابعة (مكتملة):** تم التخطيط، التصميم، تطوير الـ Backend (باستخدام نمط MVC)، وتطوير الواجهات الأمامية.
* **المرحلة الخامسة (مكتملة):** تم إعادة هيكلة المشروع ليتبع نمط **MVC Architecture** المتكامل لضمان أفضل أداء وقابلية للتطوير، مع توثيق شامل لكافة الملفات.
* **المرحلة السادسة (اختبار وإطلاق):** اختبار سرعة الموقع، أمان البيانات، وتجاوب صفحات المنتجات مع الهواتف والأجهزة الذكية، ونشر الموقع على الاستضافة المختارة.
